<%= form_for(@devan) do |f| %>
  <% if @devan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@devan.errors.count, "error") %> prohibited this devan from being saved:</h2>

      <ul>
      <% @devan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :temple %><br />
    <%= f.text_field :temple %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
